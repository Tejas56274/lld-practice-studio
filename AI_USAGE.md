# AI Usage & Collaborative Decisions

1. **Frontend UI Scaffolding:** Used AI to instantly generate a clean, responsive Tailwind CSS split-pane interface. *Accepted* because it saved hours of CSS styling and allowed focus on domain evaluation logic.
2. **Hybrid Evaluation Prompt Design:** Collaborated with the LLM to structure a prompt that outputs structured JSON containing SOLID scores and concise architectural bullet points rather than vague paragraphs. *Accepted*.
3. **Fail-Safe Fallback Mechanism:** The AI suggested wrapping the external LLM evaluation call in a try/except block with mock defaults. *Accepted* to ensure the application never crashes during a live demonstration if API limits or network issues occur.
